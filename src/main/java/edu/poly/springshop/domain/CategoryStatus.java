package edu.poly.springshop.domain;

public enum CategoryStatus {    //enum: hằng liệt kê theo mùa
    Visible, Invisible      //hiển thị và không hiển thị hoặc active -inactive love, nhập Posman status đúng tên: Visible, Invisible
}

